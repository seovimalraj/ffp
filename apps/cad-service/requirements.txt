# CAD Service Requirements
fastapi==0.116.2
uvicorn==0.35.0
python-multipart==0.0.20
pydantic==2.11.9
python-jose==3.5.0
redis==6.4.0
httpx==0.28.1

# STEP/IGES File Processing - OpenCASCADE CAD kernel
#
# NOT PIP-INSTALLABLE. pythonocc-core is published only on conda-forge (it ships
# compiled OCCT libraries), so `pip install -r requirements.txt` cannot resolve
# it and the line below is intentionally commented out. The Dockerfile installs
# it with conda, pinned to 7.9.3 (OCCT 7.9.3):
#
#     conda install -c conda-forge pythonocc-core=7.9.3 occt=7.9.3
#
# pythonocc-core==7.9.3
#
# For local development without conda, install the OCP binding instead - it is
# on PyPI and app/machining/occ.py supports both bindings transparently:
#
#     pip install cadquery-ocp   # 7.9.3.x - same OCCT as the Docker image
#
# The machining endpoint returns 503 CAD_KERNEL_UNAVAILABLE when neither is
# present; STL and DXF analysis are unaffected.

# STL File Processing
numpy-stl==3.1.1

# DXF File Processing
ezdxf==1.1.4

# DFM Analysis dependencies - numpy and trimesh for ray-casting thickness detection
numpy==1.24.3
trimesh==4.0.5
scipy==1.11.2  # Required by trimesh for mesh.sample() and spatial operations
scikit-learn==1.3.2  # ML-assisted manufacturing process classification
# networkx==3.1  # Not needed yet
